CommandConfig {
  name: String (unique)           // np. "recruit"
  description: String             // Opis komendy
  
  // Na których serwerach komenda jest dostępna domyślnie
  defaultGuilds: [String]         // np. ["ds_recruitment", "ds_main"]
  
  // Które mainRoles mogą użyć tej komendy
  allowedRoles: [String]          // np. ["enlister", "officer", "liche"]
  
  // Czy komenda wymaga dodatkowych uprawnień Discord (np. MANAGE_ROLES)
  requiresDiscordPermission: Boolean
  
  isActive: Boolean               // Czy komenda jest w ogóle aktywna
}