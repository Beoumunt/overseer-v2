GuildConfig {
  guildId: String (unique)        // ID serwera Discord
  guildName: String               // Nazwa serwera (dla czytelności)
  
  // Jakie komendy są włączone na tym serwerze
  enabledCommands: [String]       // np. ["ban", "recruit", "stats"]
  
  // Kanały konfiguracyjne
  channels: {
    logs: String                  // ID kanału logów
    memberCounter: String         // ID kanału licznika
    announcements: String         // ID kanału ogłoszeń
    recruitment: String           // ID kanału rekrutacyjnego
  }
  
  // Rangi konfiguracyjne (ID rang z tego konkretnego serwera), może być null
  roles: {
    candidate: String
    recruit: String
    darkStar: String
    merchant: String
    diplomat: String
    enlister: String
    officer: String
    liche: String
    ambassador: String
    guest: String
  }
  
  updatedAt: Date
}